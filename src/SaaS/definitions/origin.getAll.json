{
    "type": "object",
    "description": "Get instance of all origins",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "origin.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}