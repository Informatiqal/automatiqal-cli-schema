{
    "type": "object",
    "description": "Get instance of all spaces",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "space.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}