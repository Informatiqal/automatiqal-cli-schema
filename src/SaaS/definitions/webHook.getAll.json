{
    "type": "object",
    "description": "Get instance of all web hooks",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "webHook.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}