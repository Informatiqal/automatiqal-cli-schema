{
    "type": "object",
    "description": "Get instance of all API keys",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "apiKey.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}