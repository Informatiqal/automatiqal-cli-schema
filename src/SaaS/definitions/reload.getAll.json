{
    "type": "object",
    "description": "Get instance of all reloads",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "reload.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}