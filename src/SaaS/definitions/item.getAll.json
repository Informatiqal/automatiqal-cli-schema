{
    "type": "object",
    "description": "Get instance of all items",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "item.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}