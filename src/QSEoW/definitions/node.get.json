{
    "type": "object",
    "description": "Get instance of node/nodes",
    "required": [
        "name",
        "operation",
        "filter"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "node.get"
            ]
        },
        "filter": {
            "type": "string"
        },
        "skip": {
            "type": "boolean"
        }
    }
}