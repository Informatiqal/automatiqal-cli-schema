{
    "type": "object",
    "description": "Get instance of all nodes",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "node.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}