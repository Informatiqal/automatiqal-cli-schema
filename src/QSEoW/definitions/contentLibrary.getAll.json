{
    "type": "object",
    "description": "Get instance of all content libraries",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "contentLibrary.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}