{
    "type": "object",
    "description": "Get OpenAPI definitions",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "enum": [
                "about.openApi"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}