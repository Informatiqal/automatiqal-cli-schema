{
    "type": "object",
    "description": "Get instance of all systemRules",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "systemRule.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}