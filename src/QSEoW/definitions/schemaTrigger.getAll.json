{
    "type": "object",
    "description": "Get instance of all schema trigger",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "schemaTrigger.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}