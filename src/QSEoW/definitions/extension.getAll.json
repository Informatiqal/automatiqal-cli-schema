{
    "type": "object",
    "description": "Get instance of all extensions",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "extension.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}