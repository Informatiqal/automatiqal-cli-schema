{
    "type": "object",
    "description": "Get about information",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "enum": [
                "about.get"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}