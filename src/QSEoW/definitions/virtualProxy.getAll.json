{
    "type": "object",
    "description": "Get instance of all virtual proxies",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "virtualProxy.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}