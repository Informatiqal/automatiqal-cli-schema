{
    "type": "object",
    "description": "Get instance of all banner messages",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "customBannerMessage.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}