{
    "type": "object",
    "description": "Get instance of all streams",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "stream.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}