{
    "type": "object",
    "description": "Get instance of all users",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "user.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}