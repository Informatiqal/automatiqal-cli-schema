{
    "type": "object",
    "description": "Get instance of banner message(s)",
    "required": [
        "name",
        "operation",
        "filter"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "customBannerMessage.get"
            ]
        },
        "skip": {
            "type": "boolean"
        },
        "filter": {
            "type": "string"
        }
    }
}