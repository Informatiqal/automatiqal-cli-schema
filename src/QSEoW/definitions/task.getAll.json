{
    "type": "object",
    "description": "Get instance of all tasks",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "task.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}