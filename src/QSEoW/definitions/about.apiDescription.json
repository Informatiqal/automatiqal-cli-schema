{
    "type": "object",
    "description": "Get API description",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "enum": [
                "about.apiDescription"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}