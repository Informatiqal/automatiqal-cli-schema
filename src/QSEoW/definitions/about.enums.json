{
    "type": "object",
    "description": "Get enums information",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "enum": [
                "about.enums"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}