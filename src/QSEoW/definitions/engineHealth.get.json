{
    "type": "object",
    "description": "Get instance of engine(s) health",
    "required": [
        "name",
        "operation",
        "filter"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "engineHealth.get"
            ]
        },
        "skip": {
            "type": "boolean"
        },
        "filter": {
            "type": "string"
        }
    }
}