{
    "type": "object",
    "description": "Get instance of stream(s)",
    "required": [
        "name",
        "operation",
        "filter"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "stream.get"
            ]
        },
        "skip": {
            "type": "boolean"
        },
        "filter": {
            "type": "string"
        }
    }
}