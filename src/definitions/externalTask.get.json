{
    "type": "object",
    "description": "Get instance of external task/tasks",
    "required": [
        "name",
        "operation",
        "filter"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "externalTask.get"
            ]
        },
        "filter": {
            "type": "string"
        },
        "skip": {
            "type": "boolean"
        }
    }
}