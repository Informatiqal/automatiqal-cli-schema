{
    "type": "object",
    "description": "Get instance of task/tasks",
    "required": [
        "name",
        "operation",
        "filter"
    ],
    "properties": {
        "name": {
            "type": "string"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "task.get"
            ]
        },
        "filter": {
            "type": "string"
        },
        "skip": {
            "type": "boolean"
        }
    }
}