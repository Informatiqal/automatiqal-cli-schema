{
    "type": "object",
    "description": "Get instance of custom property(ies)",
    "required": [
        "name",
        "operation",
        "filter"
    ],
    "properties": {
        "name": {
            "$ref": "#/definitions/TaskName"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "customProperty.get"
            ]
        },
        "filter": {
            "type": "string"
        },
        "skip": {
            "type": "boolean"
        }
    }
}