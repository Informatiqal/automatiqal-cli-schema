{
    "type": "object",
    "description": "Get instance of all content libraries",
    "required": [
        "name",
        "operation"
    ],
    "properties": {
        "name": {
            "type": "string"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "contentLibrary.getAll"
            ]
        },
        "skip": {
            "type": "boolean"
        }
    }
}