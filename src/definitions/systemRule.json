{
    "type": "object",
    "description": "Get instance of system rule/rules",
    "required": [
        "name",
        "operation",
        "filter"
    ],
    "properties": {
        "name": {
            "type": "string"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "systemRule.get"
            ]
        },
        "filter": {
            "type": "string"
        },
        "skip": {
            "type": "boolean"
        }
    }
}