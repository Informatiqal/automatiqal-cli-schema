{
    "type": "object",
    "description": "Get instance of extension/extensions",
    "required": [
        "name",
        "operation",
        "filter"
    ],
    "properties": {
        "name": {
            "type": "string"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "extension.get"
            ]
        },
        "filter": {
            "type": "string"
        },
        "skip": {
            "type": "boolean"
        }
    }
}