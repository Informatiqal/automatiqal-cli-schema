{
    "type": "object",
    "description": "Get status of service by service id",
    "required": [
        "name",
        "operation",
        "filter"
    ],
    "properties": {
        "name": {
            "type": "string"
        },
        "operation": {
            "type": "string",
            "readOnly": true,
            "enum": [
                "serviceStatus.get"
            ]
        },
        "skip": {
            "type": "boolean"
        },
        "filter": {
            "type": "string"
        }
    }
}